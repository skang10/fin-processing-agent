diff --git a/src/lib.rs b/src/lib.rs
index f62f168..d06edb4 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -413,6 +413,8 @@ pub struct PageMarkdown {
     pub page: u32,
     /// Formatted markdown for this page.
     pub markdown: String,
+    /// Positioned native items produced by the same parse, in PDF points with a top-left origin.
+    pub native_items: Vec<TextItem>,
     /// `true` when text on this page is unreliable (GID-encoded fonts,
     /// encoding issues, garbage text, or empty extraction).
     pub needs_ocr: bool,
@@ -577,6 +579,7 @@ fn extract_pages_markdown_mem_impl(
             results.push(PageMarkdown {
                 page: page_0idx,
                 markdown: String::new(),
+                native_items: Vec::new(),
                 needs_ocr: true,
                 ocr_reason: None,
             });
@@ -649,6 +652,23 @@ fn extract_pages_markdown_mem_impl(
             ..markdown_options.clone()
         };
 
+        let mut native_items = page_items.clone();
+        let page_height = lopdf_pages
+            .get(&page_1idx)
+            .and_then(|&page_id| get_page_height(&doc, page_id))
+            .unwrap_or(792.0);
+        let coord_space = infer_region_coord_space(&native_items);
+        for item in &mut native_items {
+            let [x1, y1, x2, y2] = extracted_bbox_to_page_top_left(
+                [item.x, item.y, item.x + item.width, item.y + item.height],
+                page_height,
+                coord_space,
+            );
+            item.x = x1;
+            item.y = y1;
+            item.width = x2 - x1;
+            item.height = y2 - y1;
+        }
         let md = if has_text_quality_issue {
             String::new()
         } else {
@@ -708,6 +728,7 @@ fn extract_pages_markdown_mem_impl(
             } else {
                 md
             },
+            native_items: if needs_ocr { Vec::new() } else { native_items },
             needs_ocr,
             ocr_reason,
         });
diff --git a/src/vision/fusion.rs b/src/vision/fusion.rs
index 3872f96..b1c71a9 100644
--- a/src/vision/fusion.rs
+++ b/src/vision/fusion.rs
@@ -14,7 +14,8 @@ use crate::types::{ItemType, PdfRect, TextItem};
 use crate::PageMarkdown;
 
 use super::{
-    OcrRun, PageContentSource, PageProvenance, RoutedOcrPage, VisionTimings, DEFAULT_RENDER_DPI,
+    OcrRun, OcrSpan, PageContentSource, PageProvenance, RoutedOcrPage, VisionTimings,
+    DEFAULT_RENDER_DPI,
 };
 
 /// OCR assembly and hosted-fallback policy.
@@ -80,6 +81,9 @@ pub struct FusedPageMarkdown {
     pub page_number: u32,
     /// Final page Markdown.
     pub markdown: String,
+    /// Positioned spans returned by the same local OCR pass, in rendered-page pixels.
+    /// Empty for pages that were not recognized locally.
+    pub ocr_spans: Vec<OcrSpan>,
     /// Native/OCR source, model, timing, and fallback metadata.
     pub provenance: PageProvenance,
 }
@@ -401,6 +405,10 @@ fn fuse_ocr_pages_impl(
         pages.push(FusedPageMarkdown {
             page_number,
             markdown,
+            ocr_spans: ocr_by_page
+                .get(&page_number)
+                .map(|page| page.ocr.spans.clone())
+                .unwrap_or_default(),
             provenance: PageProvenance {
                 page_number,
                 source,
diff --git a/napi/src/lib.rs b/napi/src/lib.rs
index 7801df3..9ae8c6d 100644
--- a/napi/src/lib.rs
+++ b/napi/src/lib.rs
@@ -206,9 +206,22 @@ pub struct OcrPageResult {
     /// 1-indexed page number.
     pub page_number: u32,
     pub markdown: String,
+    /// Positioned spans from the same OCR pass. Polygon coordinates use
+    /// rendered-page pixels with a top-left origin.
+    pub spans: Vec<OcrTextSpan>,
     pub provenance: OcrPageProvenance,
 }
 
+/// One positioned OCR result in rendered-page pixel coordinates.
+#[napi(object)]
+pub struct OcrTextSpan {
+    pub text: String,
+    /// Eight values ordered x1,y1,...,x4,y4.
+    pub polygon: Vec<f64>,
+    pub confidence: f64,
+    pub orientation_degrees: Option<f64>,
+}
+
 /// Complete native/OCR Markdown output.
 #[napi(object)]
 pub struct OcrPdfResult {
@@ -330,6 +343,21 @@ fn to_napi_ocr_result(result: pdf_inspector::vision::OcrPdfResult) -> OcrPdfResu
                 OcrPageResult {
                     page_number: page.page_number,
                     markdown: page.markdown,
+                    spans: page
+                        .ocr_spans
+                        .into_iter()
+                        .map(|span| OcrTextSpan {
+                            text: span.text,
+                            polygon: span
+                                .polygon
+                                .points
+                                .into_iter()
+                                .flat_map(|point| [f64::from(point.x), f64::from(point.y)])
+                                .collect(),
+                            confidence: f64::from(span.confidence),
+                            orientation_degrees: span.orientation_degrees.map(f64::from),
+                        })
+                        .collect(),
                     provenance: OcrPageProvenance {
                         page_number: provenance.page_number,
                         source: convert_page_content_source(provenance.source),
@@ -495,33 +523,32 @@ pub fn extract_text_with_positions(
                 .map_err(|e| to_napi_err(e, "extract_text_with_positions"))?,
         };
 
-        Ok(items
-            .into_iter()
-            .map(|item| {
-                let (item_type, link_url) = convert_item_type(&item.item_type);
-                TextItem {
-                    text: item.text,
-                    x: item.x as f64,
-                    y: item.y as f64,
-                    width: item.width as f64,
-                    height: item.height as f64,
-                    font: item.font,
-                    font_tag: item.font_tag,
-                    font_size: item.font_size as f64,
-                    page: item.page,
-                    is_bold: item.is_bold,
-                    is_italic: item.is_italic,
-                    is_underline: item.is_underline,
-                    is_strikeout: item.is_strikeout,
-                    item_type,
-                    link_url,
-                    mcid: item.mcid,
-                }
-            })
-            .collect())
+        Ok(items.into_iter().map(to_napi_text_item).collect())
     })
 }
 
+fn to_napi_text_item(item: pdf_inspector::types::TextItem) -> TextItem {
+    let (item_type, link_url) = convert_item_type(&item.item_type);
+    TextItem {
+        text: item.text,
+        x: item.x as f64,
+        y: item.y as f64,
+        width: item.width as f64,
+        height: item.height as f64,
+        font: item.font,
+        font_tag: item.font_tag,
+        font_size: item.font_size as f64,
+        page: item.page,
+        is_bold: item.is_bold,
+        is_italic: item.is_italic,
+        is_underline: item.is_underline,
+        is_strikeout: item.is_strikeout,
+        item_type,
+        link_url,
+        mcid: item.mcid,
+    }
+}
+
 /// One structure-tree element reference from a tagged PDF.
 #[napi(object)]
 pub struct StructureElementJs {
@@ -857,6 +884,8 @@ pub struct PageMarkdownResult {
     pub page: u32,
     /// Formatted markdown for this page.
     pub markdown: String,
+    /// Positioned native items from the same parse, in PDF points with a top-left origin.
+    pub native_items: Vec<TextItem>,
     /// `true` when text on this page is unreliable.
     pub needs_ocr: bool,
     /// Machine-readable OCR reason when the cause is known.
@@ -914,6 +943,7 @@ fn extract_pages_markdown_impl(
             .map(|r| PageMarkdownResult {
                 page: r.page,
                 markdown: r.markdown,
+                native_items: r.native_items.into_iter().map(to_napi_text_item).collect(),
                 needs_ocr: r.needs_ocr,
                 ocr_reason: r.ocr_reason,
             })
--- a/src/vision/pipeline.rs
+++ b/src/vision/pipeline.rs
@@ -643,6 +643,7 @@
         markdown: page.markdown.clone(),
         needs_ocr: page.needs_ocr,
         ocr_reason: page.ocr_reason.clone(),
+        native_items: page.native_items.clone(),
     }
 }
 
@@ -1182,6 +1183,7 @@
                 markdown: String::new(),
                 needs_ocr: true,
                 ocr_reason: Some(crate::OCR_REASON_SCANNED.to_string()),
+                native_items: Vec::new(),
             })
             .collect::<Vec<_>>();
         let routed_pages = (1..=10).collect::<Vec<_>>();
--- a/src/vision/fusion.rs
+++ b/src/vision/fusion.rs
@@ -1068,6 +1076,7 @@
             markdown: markdown.to_string(),
             needs_ocr,
             ocr_reason: needs_ocr.then(|| "scanned".to_string()),
+            native_items: Vec::new(),
         }
     }
 
